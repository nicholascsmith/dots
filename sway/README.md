# Sway Configuration

## Network Configuration

To configure network connections, use `nmtui` (NetworkManager Text User Interface):

```bash
nmtui
```

Run this command in a terminal once all setup steps are completed to manage WiFi, ethernet, and other network connections.
